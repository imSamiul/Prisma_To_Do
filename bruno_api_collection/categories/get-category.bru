meta {
  name: Get Category
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/categories/{{categoryId}}
  body: none
  auth: none
}

docs {
  # Get Category
  Fetch one category by id. Uses `categoryId` env var.
}
